(function() {
  var countdown, deliverEggs, num;

  for (num = 10; num >= 1; num--) {
    countdown = num;
  }

  deliverEggs = function() {
    var dozen, i, _ref, _results;
    _results = [];
    for (i = 0, _ref = eggs.length; i < _ref; i += 12) {
      dozen = eggs.slice(i, (i + 12));
      _results.push(deliver(new eggCarton(dozen)));
    }
    return _results;
  };

}).call(this);
